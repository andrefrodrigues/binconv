
typedef unsigned char Bit; //here we will have the units
typedef unsigned int Decimal;
typedef unsigned int Binary;
#define MASK 0x1 //mask used for binary conversion
int binary(Decimal n); //function for converting a decimal number into binary
int decimal(Binary n); //function for converting a binary number into decimal
